// Importing necessary Firebase modules 
// Since the files are in the same project, we can import from '@/firebase/firebase'

// Correct path for the import so ../../ auth -> components -> firebase/firebase.ts 
import { auth, db } from "../../firebase/firebase"; 

