/**
 * Header Component
 *
 * @component
 * @description
 * A reusable sticky header for the stock prediction website, built with Material UI. 
 * Serves as the main navigation bar, includes logo, navigation links, and auth/admin actions.
 * Responsive: horizontal navigation on desktop, collapses to hamburger menu on mobile.
 * Navigation triggers a CustomEvent("navigate", { detail: { page } }) for decoupled routing.
 * Accessibility: keyboard navigation and ARIA attributes included.
 *
 * @example
 * <Header />
 *
 * @props None
 */